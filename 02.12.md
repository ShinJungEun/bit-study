< Linux >

* application은 system call을 통해 kernel 접근

* VM network 설정: 어댑터에 브리지

#ifconfig

#sync
#shutdown -h now

#yum update

#ps -ef | grep sshd

#pwd : 현재 위치 확인

#echo : 한줄 출력가능 ex) echo 'hello'

*shell
 - 입력모드 : i
 - 저장하고 나가기 : esc -> :wq!

#ls -l

#chmod 755 파일명 : file에 실행권한을 줌 

* time_sync.sh => 현재 시간 맞추기

* #cp 복사할파일(위치포함) .(현재디렉토리)
 ex) #cp /root/time_sync.sh .

* update : source /etc/profile

#history : root에서 쳤던 명령어들 목록 출력



* ignore package
 - src, pom(xml문서)만 git에 업로드
 - 어떠한 환경에서도 git에서 다운받으면 사용 가능

* maven
 - C:\helloworld2 c>mvn clean package


* git disconnect : team -> disconnect => Git Repository의 package -> delete repository(history만 check) 